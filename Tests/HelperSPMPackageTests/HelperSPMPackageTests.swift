import XCTest
@testable import HelperSPMPackage

final class HelperSPMPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HelperSPMPackage().text, "Hello, World!")
    }
}
